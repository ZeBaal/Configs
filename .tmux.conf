set -g default-terminal "screen-256color" 
set-option -g default-shell "/usr/bin/fish"

#ctrl-a
unbind C-b
set -g prefix C-a
bind C-a send-prefix

unbind %
bind | split-window -h
bind - split-window -v
